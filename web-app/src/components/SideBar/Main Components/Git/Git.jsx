import React from 'react'

const Git = (props) => {
  return (
    <div id="gitarea" className={`sidebarscontent d-${props.ariaExpandedisplaygit}`}>
      <h6>Git</h6>
    </div>
  )
}

export default Git
